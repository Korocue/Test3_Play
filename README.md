# Test3_Play

シンプルな数当てゲームを楽しめる小さなリポジトリです。`python -m src.game` を実行すると、1〜100 の数字を 8 回以内に当てるチャレンジが始まります。

## 遊び方
1. ターミナルでプロジェクトルートに移動します。
2. `python -m src.game` を実行します。
3. 表示されるヒントを頼りに数字を入力してください。数字以外を入力すると再入力を促されます。

## テスト
`pytest` で簡単な動作確認ができます。

```bash
python -m pytest
```
